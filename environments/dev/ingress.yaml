# Web Ingress for development environment
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kamf-dev-web-ingress
  labels:
    app: kamf-web
    environment: development
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - dev.kamf.site
      secretName: kamf-dev-tls-web
  rules:
    - host: dev.kamf.site
      http:
        paths:
          # Web routes - catch all non-API paths
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kamf-web-service
                port:
                  number: 3000
---
# API Ingress for development environment
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kamf-dev-api-ingress
  labels:
    app: kamf-api
    environment: development
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  tls:
    - hosts:
        - dev.kamf.site
      secretName: kamf-dev-tls-api
  rules:
    - host: dev.kamf.site
      http:
        paths:
          # API routes - handle /api prefix stripping with regex capture
          - path: /api(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: kamf-api-service
                port:
                  number: 8000
