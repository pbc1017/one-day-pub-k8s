# MySQL hostPort patch for DEV environment  
# Exposes MySQL on host port 23306
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
spec:
  template:
    spec:
      containers:
      - name: mysql
        ports:
        - containerPort: 3306
          name: mysql
          hostPort: 23306
