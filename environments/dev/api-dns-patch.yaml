apiVersion: apps/v1
kind: Deployment
metadata:
  name: one-day-pub-api
spec:
  template:
    spec:
      dnsConfig:
        searches:
        - "one-day-pub-dev.svc.cluster.local"
        - "svc.cluster.local"
        - "cluster.local"
