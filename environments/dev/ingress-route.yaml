# Traefik IngressRoute for development environment
# This will be used after complete migration from nginx
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: one-day-pub-dev-web
  labels:
    app: one-day-pub-web
    environment: development
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`dev.one-day-pub.site`)
      kind: Rule
      services:
        - name: one-day-pub-web-service
          port: 3000
  tls:
    certResolver: letsencrypt
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: one-day-pub-dev-api
  labels:
    app: one-day-pub-api
    environment: development
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`dev.one-day-pub.site`) && PathPrefix(`/api`)
      kind: Rule
      services:
        - name: one-day-pub-api-service
          port: 8000
      middlewares:
        - name: dev-api-stripprefix
  tls:
    certResolver: letsencrypt
---
# Middleware to strip /api prefix
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: dev-api-stripprefix
spec:
  stripPrefix:
    prefixes:
      - /api
