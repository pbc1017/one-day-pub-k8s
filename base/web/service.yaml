apiVersion: v1
kind: Service
metadata:
  name: kamf-web-service
  labels:
    app: kamf-web
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: kamf-web